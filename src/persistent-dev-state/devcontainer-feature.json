{
    "name": "Persistent Dev State",
    "id": "persistent-dev-state",
    "version": "1.0.8",
    "description": "Persist configured filesystem paths in a named volume that survives rebuilds",
    "options": {
        "paths": {
            "type": "string",
            "default": "",
            "description": "Semicolon-separated list of paths to persist. Each entry must be an absolute path or begin with ~ (for home expansion)."
        }
    },
    "containerEnv": {
        "PERSISTENT_DEV_STATE_ROOT": "/var/lib/devcontainer-state"
    },
    "mounts": [
        {
            "type": "volume",
            "source": "devcontainer-state-${devcontainerId}",
            "target": "/var/lib/devcontainer-state"
        }
    ],
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils"
    ]
}
